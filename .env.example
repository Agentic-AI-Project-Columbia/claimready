GCP_PROJECT_ID=agentic-ai-487000
GCP_REGION=us-east1
AGENT_MODEL=vertex_ai/gemini-2.5-flash
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Optional: API key auth (leave empty to disable)
API_KEY=
NEXT_PUBLIC_API_KEY=

# Optional: GCS bucket for evidence persistence (leave empty for local-only)
GCS_EVIDENCE_BUCKET=

# Optional: OpenTelemetry tracing (leave empty to disable)
OTEL_EXPORTER_OTLP_ENDPOINT=
